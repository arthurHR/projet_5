    {{ form_start(form, {'attr': {'id': 'form_skill_update'}})}}
        {{ form_row(form.title, { 'label': 'Titre' }) }}
        {{ form_row(form.imageFile, { 'label': 'Changer l\'image' }) }}
        <button type="submit" name="submitSkill" class="btn btn-primary" data-label="Enregistrer">
            Enregistrer
        </button>
    {{ form_end(form) }}
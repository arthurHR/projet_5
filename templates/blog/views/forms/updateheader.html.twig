{{ form_start(form, {'attr': {'id': 'form_header'}})}}
    {{ form_row(form.title, { 'label': 'Titre' }) }}
    {{ form_row(form.subtitle,{ 'label': 'Sous-titre' }) }}

    <button type="submit" name="submitheader" class="btn btn-primary" data-label="Enregistrer">
        Enregistrer
    </button>
{{ form_end(form) }}

